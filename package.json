{
  "name": "babycare-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ensure:data": "node -e \"const fs=require('node:fs'),path=require('node:path');const D=process.env.DATA_DIR||'./data';const dir=path.resolve(D,'uploads','avatars');try{fs.mkdirSync(dir,{recursive:true});try{fs.accessSync(dir,fs.constants.W_OK);console.log('[ensure:data] OK  %s',dir)}catch(e){console.error('[ensure:data] 目录不可写：%s（%s）。请在容器里给 DATA_DIR 挂可写 volume 或 chown/chmod',dir,e.message);process.exit(1)}}catch(e){console.error('[ensure:data] 无法创建：%s（%s）',dir,e.message);process.exit(1)}\"",
    "predev": "npm run ensure:data",
    "prestart": "npm run ensure:data",
    "prebuild": "npm run ensure:data",
    "dev": "concurrently -k \"npm:dev:web\" \"npm:dev:server\"",
    "dev:web": "vite --host 127.0.0.1",
    "dev:server": "tsx watch server/index.ts",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.server.json --noEmit",
    "build": "npm run typecheck && vite build && tsc -p tsconfig.server.json",
    "start": "node dist-server/server/index.js",
    "test": "vitest run",
    "check": "npm run test && npm run build && npm run check:css",
    "check:css": "node scripts/check-css.mjs",
    "images:check": "python3 scripts/optimize-images.py",
    "images:optimize": "python3 scripts/optimize-images.py --apply"
  },
  "dependencies": {
    "axios": "^1.19.0",
    "better-sqlite3": "^13.0.3",
    "cookie-parser": "^1.4.7",
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "lucide-react": "^1.31.0",
    "multer": "^2.2.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "sharp": "^0.35.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/cookie-parser": "^1.4.10",
    "@types/express": "^5.0.6",
    "@types/multer": "^2.2.0",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "concurrently": "^10.0.4",
    "tsx": "^4.23.11",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
